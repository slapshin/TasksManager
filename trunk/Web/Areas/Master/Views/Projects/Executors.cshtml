@using Translation = Model.Enums.Translation

@model ICollection<string>

<h2>Участники
    @Html.ActionLink(" ", "ExecutorsManagement", new { id = ViewBag.ProjectId }, new { @class = "icon-pencil", title = "Редактировать" })
</h2>

@if (Model.Count > 0)
{         
    <ul class="unstyled">
        @foreach (string exec in Model)
        { 
            <li>
                <i class="icon-user" style="margin-right: 5px"></i>@exec
            </li>            
        }
    </ul>                
}

