
@if (@User.Identity.IsAuthenticated)
{        
    <ul class="nav navbar-nav">
        @if (User.IsAdmin())
        {
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Администрирование <b class="caret"></b></a>
                <ul class="dropdown-menu">
                    <li><a href="#/admin/users/list">Пользователи</a></li>
                    <li><a href="#/admin/projects/list">Проекты</a></li>                    
                </ul>
            </li>
        }
        @if (User.IsCustomer())
        {
        }

        @if (User.IsRouter())
        {
        }

        @if (User.IsMaster())
        {
        }

        @if (User.IsExecutor())
        {

        }
    </ul>
}
